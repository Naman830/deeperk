{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Playwright specs, not vitest ones — no vitest globals here.
    "types": ["node"]
  },
  "include": [".", "../playwright.config.ts"]
}
