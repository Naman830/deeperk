{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end harness: drives the real Next + socket servers against the real Neon DB.",
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "dotenv": "^17.2.4",
    "socket.io-client": "^4.8.3",
    "vitest": "^3.2.4"
  }
}
